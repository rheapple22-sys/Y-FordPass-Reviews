# Car Apps Reviews — Streamlit Analytics

Run the interactive analytics app locally:

```bash
pip install -r requirements.txt
streamlit run streamlit_app.py
```

The app expects `car_apps_reviews.csv` in the repository root (same folder).
# Y-FordPass-Reviews
Objective : Analyze user reviews of the FordPass mobile application to:
Understand customer sentiment
Identify key problem categories
Track trends over time
Provide actionable insights for product & CX teams
